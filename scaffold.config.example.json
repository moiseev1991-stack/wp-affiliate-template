{
  "_comment": "Copy this file to scaffold.config.json and fill in real values. Then run: node scripts/scaffold.mjs",

  "_link_policy": "Every site produced by the scaffold ships with a hard-capped money-link policy: 3 anchors on the home page (in featured cards 1/4/7) + 1 link in the first paragraph of each of the 5 money articles = 8 outbound money links across 6 pages, period. Every other article carries 0 outbound money links and shows the brand as plain text. Enforced at render time by lib/internal-links.ts; cannot be bypassed by the article generator.",


  "domain": "example.pl",
  "language": "pl",
  "languageName": "Polish",
  "niche": "casino",
  "siteName": "Example",
  "tagline": "",
  "description": "",

  "money": {
    "url": "https://vulkankasyno.pl",
    "anchor": "Vulkan Vegas",
    "bonus": "100% bonus do 4000 PLN"
  },

  "donor": "https://competitor.pl",

  "repo": "moiseev1991-stack/example-pl",

  "pages": 50,

  "_env_vars_required": [
    "OPENAI_API_KEY      — used to generate the initial 50 articles AND the monthly cron",
    "GITHUB_TOKEN        — used to push to <repo> and set repo secrets (or fall back to gh CLI)",
    "CLOUDFLARE_API_TOKEN  — optional, enables auto-deploy to Cloudflare Workers",
    "CLOUDFLARE_ACCOUNT_ID — optional, paired with CLOUDFLARE_API_TOKEN",
    "ANTHROPIC_API_KEY   — optional fallback for article generation if OPENAI_API_KEY is absent"
  ],

  "_optional_overrides": {
    "moneyArticleTitles": null,
    "openaiModel": "gpt-4o-mini",
    "skipDeploy": false,
    "skipBuild": false
  }
}
